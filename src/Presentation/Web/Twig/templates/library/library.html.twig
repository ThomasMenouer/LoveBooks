{% extends 'base.html.twig' %}

{% block title %}library{% endblock %}

{% block body %}
<main>
    <div class="container my-4">
        <div 
            {{ react_component('LibraryApp', {
                userId: user.id,
                isOwnProfile: app.user and app.user.id == user.id,
            }) }}>
        </div>
    </div>
</main>
{% endblock %}