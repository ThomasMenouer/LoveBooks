{% extends 'base.html.twig' %}

{% block title %}Edit Profile{% endblock %}

{% block body %}

<main>
    <div class="container">
        <div class="row">
            {{ form_start(form) }}
                {{ form_row(form.avatar) }}
                {{ form_row(form.submit) }}
            {{ form_end(form) }}
        </div>

    </div>
</main>

{% endblock %}

