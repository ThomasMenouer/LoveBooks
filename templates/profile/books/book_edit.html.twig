{% extends 'base.html.twig' %}

{% block title %}Modifier un livre{% endblock %}

{% block body %}
    <h1>Modifier un livre</h1>

    {{ form_start(form) }}
        {{ form_row(form.title) }}
        {{ form_row(form.authors) }}
        {{ form_row(form.status) }}
        {{ form_row(form.submit) }}
    {{ form_end(form) }}

    <a href="{{ path('profile_book_details', {'id': book.id}) }}" class="btn btn-outline-custom">Annuler</a>
{% endblock %}