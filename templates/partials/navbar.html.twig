<aside>
    <nav class="nav flex-column sidebar">
    {% if app.user %}
    
        <a class="nav-link text-white" href="{{ path("mybooks_index")}}">My Books</a>

        <a class="nav-link text-white" href="{{ path("search_book")}}">Search a book</a>

        <a class="nav-link text-white" href="{{ path("app_logout")}}">Déconnexion</a>

    {% else %}

        <a class="nav-link text-white" href="{{ path("app_login")}}">Me connecter</a>

        <a class="nav-link text-white" href="{{ path("app_register")}}">M'inscrire</a>

    {% endif %}
</nav>

</aside>